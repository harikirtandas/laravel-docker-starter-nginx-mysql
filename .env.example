# Variables opcionales para docker-compose.yml (no confundir con src/.env, que es el .env de Laravel).
# Copiá este archivo a .env si querés cambiar algún default. No es necesario para que `make install` funcione.

APP_PORT=8000
DB_DATABASE=laravel
DB_USERNAME=laravel
DB_PASSWORD=secret
